spring:
  application:
    name: product-service
  cloud:
    config:
      discovery:
        service-id: CONFIG-SERVICE
        enabled: true
      profile: test
      label: master

eureka:
  client:
    serviceUrl:
      defaultZone: http://192.168.2.213:8761/eureka/